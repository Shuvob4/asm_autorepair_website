FEATURE_BOOKING_ENABLED=false
FEATURE_EMAIL_ENABLED=false

# Email notification for quote form (Netlify serverless function)
RESEND_API_KEY=
SHOP_EMAIL=hossainliyana@gmail.com

# Google Sheet CSV URL for post content pipeline
# Publish your Google Sheet as CSV: File → Share → Publish to web → CSV
GOOGLE_SHEET_CSV_URL=

# Google Sheet webhook for quote tracking (Google Apps Script web app URL)
# See docs/quote-sheet-setup.md for setup instructions
GOOGLE_SHEET_WEBHOOK_URL=
